### 字库生成

1. 在out目录下编译字库生成程序
~~~
make dictbuild
~~~

2. 将输出的字库生成程序拷贝到目标机器上

3. 拷贝`cdroid/src/3rdparty/pinyin/data`到目标机器上，注意该文件夹需与字库生成程序在同一目录下

4. 在目标机器上运行字库生成程序，即可产生对应架构的字库文件
~~~
./dictbuild
~~~

### 字库使用

1. 将生成的字库文件拷贝到主程序同目录下的`pinyin`文件夹下

2. 运行主程序，即可加载字库文件
